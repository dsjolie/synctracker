chiptune3 0.8.9 (npm `chiptune3`, https://github.com/DrSnuggles/chiptune), copied unmodified.
MIT; the compiled libopenmpt in `libopenmpt.worklet.js` is under the OpenMPT BSD licence.

The three .js files must stay together: `chiptune3.js` loads the worklet relative to
`import.meta.url`.
